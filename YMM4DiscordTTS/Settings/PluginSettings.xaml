<UserControl x:Class="YMM4DiscordTTS.Settings.PluginSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YMM4DiscordTTS.Settings"
             xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Focusable="True" >
        <Expander Header="プラグイン設定" IsExpanded="True">
            <GroupBox Header="バージョン" Padding="5" Margin="5">
                <StackPanel>
                    <Grid>
                        <Label Content="現在のバージョン" VerticalAlignment="Center"/>
                        <TextBlock x:Name="VersionTextBlock"
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Center"
                   Opacity="0.7"/>
                    </Grid>

                    <Grid>
                        <Label Content="起動時にアップデートを確認する"/>
                        <c:ToggleSlider Value="{Binding Source={x:Static local:TTSSettings.Default},Path=IsCheckVersion}" HorizontalAlignment="Right"/>
                    </Grid>
                </StackPanel>
            </GroupBox>
        </Expander>

        <local:TTSSettingsView/>
    </StackPanel>
</UserControl>
